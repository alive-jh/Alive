package com.wechat.jfinal.model;

import java.util.List;

import com.jfinal.plugin.activerecord.Db;
import com.wechat.jfinal.model.base.BaseMallProductCollector;

/**
 * Generated by JFinal.
 */
@SuppressWarnings("serial")
public class MallProductCollector extends BaseMallProductCollector<MallProductCollector> {
	public static final MallProductCollector dao = new MallProductCollector().dao();

	public List<MallProductCollector> findByStudent(Integer student) {
		return find("select * from mall_product_collector where student_id = ? and `status` = 1", student);
	}

	public void update(int student, int product) {
		Db.update(
				"INSERT INTO `mall_product_collector` (`student_id`,`mall_product_id`) VALUES (?,?) ON DUPLICATE KEY UPDATE `status` = ABS(`status`-1)",
				student, product);
	}
}
